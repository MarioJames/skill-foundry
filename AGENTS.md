# 父任务调度

当你是父任务所有者时，每次收到已投递的新指令、拆分出独立交付项或子任务完成/失败结果，都应在继续长步骤或阻塞等待前读取本仓库 `skills/agent-dispatch/SKILL.md` 并执行其轻量判断；已读取时直接沿用。就绪且有收益的独立工作及时派发。Jev 判断是否及如何并行；`agents.json` 存在 override 时跳过难度路由。受委派工作者不得递归派发。

本规则只补充当前仓库的技能发现；用户明确要求的执行范围和工具优先。
