# 父任务调度

当你是父任务所有者时，首次接到包含多个交付项的任务，以及每次收到已投递的新增/变更指令、拆分出独立交付项或子任务完成/失败结果，都应在继续长步骤或阻塞等待前读取本仓库 `skills/agent-dispatch/SKILL.md` 并执行其轻量判断；已读取时直接沿用。盘点当前工作、未推进待办和积压问题，把工作全貌交给 Jev 判断哪些可独立执行；父任务提供依赖、资源及权限事实，不预先按主观收益筛选。根据结果启动 JSON-RPC 后台任务或 Herdr pane；仅有不可再拆的当前操作且无积压，或硬检查阻止所有组合时，说明未调用的原因。`agents.json` 存在 override 时跳过难度路由。受委派工作者不得递归派发。

本规则只补充当前仓库的技能发现；用户明确要求的执行范围和工具优先。
