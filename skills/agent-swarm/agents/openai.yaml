interface:
  display_name: "Agent Swarm (Legacy Alias)"
  short_description: "TypeScript compatibility alias for Agents Orchestrator"
  default_prompt: "Use $agent-swarm to run this task with the canonical Runtime in swarm mode."

policy:
  allow_implicit_invocation: false
