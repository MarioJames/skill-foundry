interface:
  display_name: "Agent Dispatch"
  short_description: "追加指令后盘点当前工作与积压问题，由 Jev 判断并行，再启动 RPC 或 Herdr"
  default_prompt: "使用 $agent-dispatch 先盘点当前与追加需求中的全部未完成工作，把当前工作与积压问题交给 Jev 判断未推进工作能否独立执行，再按结果启动 JSON-RPC 或 Herdr；硬阻塞时说明具体原因，区分未调用与置信度未达门槛。"
policy:
  allow_implicit_invocation: true
