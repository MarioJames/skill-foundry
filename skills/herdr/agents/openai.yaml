interface:
  display_name: "Herdr"
  short_description: "Name tabs at task start, even for read-only or solo work"
  default_prompt: "Use $herdr at conversation start or a main-task change to name your own tab, including for read-only questions without parallel work, then coordinate useful parallel work and clean up task-owned resources."

policy:
  allow_implicit_invocation: true
