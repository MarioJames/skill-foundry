interface:
  display_name: "Herdr"
  short_description: "Add task routing to an active development Agent"
  default_prompt: "Use $herdr to route independent additions while keeping the current Agent on its own concrete development slice."

policy:
  allow_implicit_invocation: true
