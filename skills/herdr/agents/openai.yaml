interface:
  display_name: "Herdr"
  short_description: "Route tasks across the current Agent and Herdr lanes"
  default_prompt: "Use $herdr to decide whether this request stays in the current Agent or needs an independent Herdr lane, then manage its ownership and lifecycle."

policy:
  allow_implicit_invocation: true
