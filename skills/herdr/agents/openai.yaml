interface:
  display_name: "Herdr"
  short_description: "Name task tabs and coordinate parallel work in Herdr"
  default_prompt: "Use $herdr to name your own tab and update it when the main task changes, parallelize work where it shortens the critical path, integrate the results, and clean up task-owned resources."

policy:
  allow_implicit_invocation: true
