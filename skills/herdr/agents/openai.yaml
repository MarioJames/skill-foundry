interface:
  display_name: "Herdr"
  short_description: "命名当前标签页，结合 Jev 选择多任务并行方案与执行模型，并管理任务资源"
  default_prompt: "使用 $herdr 命名当前标签页；对有收益的多任务工作拆分依赖和写入范围，按需通过 Jev 选择并行方案及执行模型，验收集成结果并清理任务自有资源。"

policy:
  allow_implicit_invocation: true
