interface:
  display_name: "Herdr"
  short_description: "在会话开始或主任务变化时命名标签页，并协调有收益的并行工作和任务资源清理"
  default_prompt: "使用 $herdr 在会话开始或主任务变化时命名当前标签页，包括只读问答和单任务工作；按需协调并行工作并清理任务自有资源。"

policy:
  allow_implicit_invocation: true
