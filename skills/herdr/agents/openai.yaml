interface:
  display_name: "Herdr"
  short_description: "Prioritize useful parallel work through Herdr"
  default_prompt: "Use $herdr to parallelize work where it shortens the critical path, integrate the results, and clean up task-owned resources."

policy:
  allow_implicit_invocation: true
