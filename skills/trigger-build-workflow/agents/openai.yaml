interface:
  display_name: "Trigger Build Workflow"
  short_description: "Commit, push, or build within authorized scope"
  default_prompt: "Use $trigger-build-workflow to perform only the commit, push, or GitHub Actions build/release operations authorized in this request; workflow compatibility does not authorize additional actions."
